{
  "name": "deco-restful",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:node": "npx esbuild ./src/index.mts --bundle --outfile=dist/index.js --platform=node --sourcemap",
    "build:types": "npx dts-bundle-generator src/index.mts -o dist/index.d.ts",
    "build": "npm run build:node && npm run build:types",
    "dev": "nodemon --exec tsx ./src/demo/exporess-demo.mts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.4.0",
    "express": "^5.2.1",
    "nodemon": "^3.1.14",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
